module.exports = {
  apps: [
    {
      name: "retell-agent",
      script: "./server.js",
      instances: 1,
      autorestart: true,
      watch: false,
      env: {
        NODE_ENV: "production",
        PORT: process.env.PORT || 3000,
        HR_TOKEN: process.env.HR_TOKEN,
        HR_ID: process.env.HR_ID
      }
    }
  ]
};
