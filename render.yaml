services:
  - type: web
    name: retell-agent
    env: node
    plan: free
    buildCommand: "npm install"
    startCommand: "npm run render-start"
    envVars:
      - key: TOOL_SECRET
        sync: false
      - key: PORT
        value: 10000
