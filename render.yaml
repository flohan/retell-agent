services:
  - type: web
    name: retell-agent
    env: node
    region: frankfurt
    plan: free
    buildCommand: npm install
    startCommand: npm run render-start
    autoDeploy: true
    envVars:
      - key: TOOL_SECRET
        sync: false
    healthCheckPath: /healthz

